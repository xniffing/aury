You are writing a Python program.

Output **only** the Python source — no prose, no markdown fences, no
explanation. Define a top-level function with exactly this signature so an
automated harness can import and call it:

- name: `{{FN}}`
- parameters (in order): {{PARAMS}}
- returns: an integer

## Task

{{INTENT}}

Define exactly the function `{{FN}}` and nothing else is required (helper
functions are allowed). All arguments are integers; return an integer. Do not
read input or print anything — just return the value.

Output the function now.
